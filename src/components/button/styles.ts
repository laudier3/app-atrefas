import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    btn: {
        justifyContent: "center",
        width: "80%",
        backgroundColor: "orange",
        padding: 10,
        borderRadius: 10,
        boxShadow: "0 0 0 1 gray",
    },

    txt: {
        fontSize: 20,
        color: 'white',
        textAlign: "center",
        fontWeight: "bold"
    }
})